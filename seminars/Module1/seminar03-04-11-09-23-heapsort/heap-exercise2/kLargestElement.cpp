#include <iostream>
#include <vector>

// Returns the kth largest element from a vector of positive integers
int kthLargest(std::vector<int>& nums, int k)
{
    // Implement your solution using the HEAP class
}

int main()
{
    std::vector<int> nums = {3, 2, 1, 5, 6, 4};

    // Prints the second largest element in nums = 5
    std::cout << kthLargest(nums, 2) << std::endl;

    // Prints the fourth largest element in nums = 3
    std::cout << kthLargest(nums2, 4) << std::endl;

    return 0;
}